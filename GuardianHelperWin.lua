GuardianHelperWin = class(Turbine.UI.Window);

function GuardianHelperWin:Constructor()
    Turbine.UI.Window.Constructor(self);

    -- Setup the window elements:
    -- For now, show all the skills in fixed position.
    -- Later, we can conditionally hide the ones that are not available.
    

end
